import React from 'react'

import {Title, NavigationBar, Screen} from '@shoutem/ui'

const UserScreen = (props) => {
    return (
    <Screen>
    <NavigationBar styleName="inline" title="MY PROFILE"/>
    </Screen>
    )
}

export default UserScreen